//========================================
// >> SWITCH LANGUAGE
//========================================
if (config.GetLANG(config.LANG.ENGLISH))
{
    // ENGLISH
    $(`#nav-center > span`).text("Forum")
}
else
{
    // KOREAN
    $(`#nav-center > span`).text("포럼")
}